import styled from 'styled-components'

export const Buttons = styled.section`
  display: flex;
  place-items: center;
  place-content: center;
  flex-flow: column;
  gap: 12px;

  > button {
    width: 100%;
    height: 40px;
    padding: 0 16px;

    font-size: 16px;
  }
`
